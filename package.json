{
  "name": "02_elevator",
  "version": "1.0.0",
  "description": "A vanilla JS elevator simulation app.",
  "scripts": {
    "start": "live-server --port=3000 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
